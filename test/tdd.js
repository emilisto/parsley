var Parsley = require('..')
var mocha = require('mocha');

Parsley.config.redis = { port: 16379 };

//describe('Parsley.Canvas', function() {

  //var worker;

  //before(function() { worker = new Parsley.Worker(); });
  //beforeEach(function() { worker.start(); });
  //afterEach(function() { worker.end(); });

  

  ////it('should take less than 500ms', function(done){
    ////this.timeout(500);
    ////setTimeout(done, 300);
  ////})

  ////it('should take less than 500ms as well', function(done){
    ////this.timeout(500);
    ////setTimeout(done, 200);
  ////})

//})

//suite('Parsley.Canvas', function(){
  //setup(function(){
    //console.log('setup');
    //// ...
  //});

  //before(function() { worker = new Parsley.Worker(); });
  //beforeEach(function() { worker.start(); });
  //afterEach(function() { worker.end(); });



  //suite('#indexOf()', function(){
    //test('should return -1 when not present', function(){
      //assert.equal(-1, [1,2,3].indexOf(4));
    //});
  //});
//});

